# The installer creates the live owner-only environment file with generated
# credentials and current-user XDG paths. This file documents required keys.
ADMIN_TOKEN=
PROXY_API_KEY=
CONFIG_PATH=
AGENT_PROXY_DATABASE_PATH=
AGENT_PROXY_HOST=127.0.0.1
AGENT_PROXY_PORT=8300
SHUTDOWN_TIMEOUT_MS=30000
